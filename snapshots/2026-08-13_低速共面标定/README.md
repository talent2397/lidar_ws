# 代码快照 2026-08-13（低速共面标定）

当前 main 的 08-13 版本快照：

- 最终外参已写入 `spherical_robot.urdf` 与 `suspension_compensator.py`；
- 新增三个离线标定脚本：`calibrate_ground_coplanar.py`、
  `calibrate_planes_offline.py`、`calibrate_icp_offline.py`；
- `tuned_calib.yaml` 为本次手动调参结果；
- `1.jpg` / `2.jpg` 为标定前两帧点云错位截图（诊断用）。

工作区文件才是最新版，本目录只做存档。
