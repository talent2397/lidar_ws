# 双雷达点云穿透问题 — 问题记录与解决方案

> **当前状态（2026-08-05）**：全链路验证通过 —— 静止段 0.06% 穿透、
> 运动段 0.24%（最新 bag 102121）、输出 4.43Hz、地面全程水平（倾斜 <0.4°）。
> 动态补偿 v3 + C++ 融合节点 + 球心高度 345mm 修正全部生效。
> 进度入口见 [PROGRESS.md](PROGRESS.md)，
> bag 数据汇总见 [BAG分析汇总.md](BAG分析汇总.md)，
> 代码快照在 `snapshots/2026-08-05_最终102121/`。

> 日期：2026-08-04
> 环境：ROS 2 Humble（Jetson ARM64），双 RoboSense Airy 雷达，球式机器人
> 工作目录：`/home/wz/lidar_ws`

## 1. 问题现象

- `/merged_points` 点云经常穿透到 grid / 地面以下，运动时尤其严重（z_min 最深可达 -11.5 m）。
- 之前还观察到静止时 lidar1 的点云“很飘”。
- 原始点云 `/rslidar_points_1`、`/rslidar_points_2` 在各自雷达坐标系下是干净的（z_min ≈ 0.046~0.048 m），因此所有穿透都来自 TF 变换，不是雷达数据本身的问题。

## 2. 分析结论

### 2.1 静态外参偏差（已解决）

- 旧外参下，用 IMU 加速度计重力方向 + 点云地面拟合交叉验证：两个雷达相对 world 的倾斜约 11.2° / 12.5°，这就是静止时也穿透的原因。
- 用 20260804_160126 bag 的静止段（每雷达 80 帧，约 650 万点）拟合地面平面后，更新了 URDF 外参。
- 新外参在静止段验证：地面拟合 `z = -0.0058x + 0.0037y + 0.0033`，倾斜 0.39°，offset 3 mm，基本完美。

### 2.2 运动时穿透的真正原因（当前结论）

新 bag `dual_lidar_20260804_161906`（121 帧 merged，纯静态 TF）：

| 阶段 | 结果 |
|---|---|
| 静止段（前 30 s） | 地面在 z≈0，倾斜 0.39°；仅 0.04% 点 < -0.2 m |
| 运动段（30~60 s） | 8~16% 点 < -0.2 m，z_min 最深 -11.5 m；地面整体上移约 0.23 m（传感器被压低），一侧出现动态倾斜 |
| 停止后（60~73 s） | 陀螺归零（\|ω\|≈0.02）、\|a\|≈1g，但穿透未恢复：2~8% 点 < -0.2 m |

IMU 重力方向验证：运动前后两个 lidar 相对初始姿态**永久偏了 7.25° / 7.24°**，与点云残余倾斜吻合。

**结论：** 新静态标定本身是对的；剩余穿透是因为球滚动/悬挂回弹导致 lidar 相对 world 的姿态和高度在运动中变化，且运动结束后不回到原位。静态 TF 无法解决，必须用 lidar 内置 IMU 做动态补偿（把 lidar 的姿态实时变换到 world 下）。

## 3. 遇到的问题与解决方案

### 3.1 rosbag2 录制崩溃（SQLite error 1299）

- 现象：`ros2 bag record` 启动后立即崩溃，只留下 `.db3` 没有 `metadata.yaml`，报 `SQLite error (1299): constraint failed`。
- 原因：rosbag2 0.15.16 已知偶发 bug（[ros2/rosbag2#1971](https://github.com/ros2/rosbag2/issues/1971)）。
- 解决：
  - `record_bag.sh` 增加自动重试：失败后把残包移到 `bags/_broken/`，自动重录（最多 5 次）。
  - 残包保留在 `bags/_broken/`（未删除，但不可用于分析）。
  - 备选方案：`sudo apt install ros-humble-rosbag2-storage-mcap` 改用 mcap 存储（尚未启用）。

### 3.2 动态补偿旋转公式错误

- 现象：开启 suspension_compensator 后动态 TF 漂移到 170°。
- 原因：`rotate_vector_by_quat()` 符号错误，180° 旋转被算成恒等变换。
- 解决：重写四元数旋转公式，用 scipy 验证与标准实现一致。

### 3.3 静态外参偏差 11~12°

- 解决：自动地面平面标定 → 更新 URDF（见第 4 节数值）。

### 3.4 suspension_compensator 设计缺陷（本次修改）

原 v2 版本有三个问题：

1. **静止时把累计旋转归零**：对球式机器人是错的——球滚完后 lidar 不回到初始姿态，归零会删掉刚需要的补偿。
2. **只补偿旋转，不补偿 z 方向回弹**：运动时传感器被压低约 0.23 m。
3. **只用陀螺积分，没有用加速度计校正漂移**；静止检测只看角速度，垂直回弹测不到。

**v3 方案：**

- 取消“静止归零”，保留累计姿态；
- 静止时用加速度计重力方向做慢速漂移校正（互补滤波）；
- 增加 z 方向回弹补偿（泄漏双积分，可调参数，默认开启）；
- 陀螺零偏只在低运动时更新（收敛加快到 ~2.5s）；
- 校正方向经过离线仿真验证：`q_accum ⊗ q_corr⁻¹`（右乘共轭），直接右乘会把角度反向放大；
- 加速度计校正加 0.5° 死区，避免与静态标定的微小差异打架。

**v3 离线仿真结果**（用 161906 bag 的 IMU 数据回放）：

- 静止段：漂移约 1.2°（主要是加速度计无法观测的 yaw），z 补偿接近 0；
- 运动段：陀螺积分跟踪实际姿态（包含球体滚动产生的大角度 yaw）；
- 停止后：姿态与加速度计重力方向偏差在几秒内收敛到 ~0.6°，z 补偿回到 0 附近；
- 已知限制：yaw 方向漂移无法用加速度计校正（需要磁力计/点云特征，后续再做）；z 补偿对持续压缩（实测约 23cm）跟踪有限，只能部分补偿回弹振荡。

### 3.5 融合节点没有按点云到达时刻查 TF（v3 验证后新发现）

用 164735 bag（动态补偿开启）验证 v3 后，运动段穿透减半（%点<-0.2m 从 8.3% → 4.2%），
但未消除。离线分析发现：

- 穿透与动态 TF 摆动速度强相关：TF 旋转速率 <2°/s 时仅 0.03% 点穿透；
  10~100°/s 时升到 3.8~4.7%。
- 原因：`point_cloud_fusion.py` 用 `rclpy.time.Time()`（处理瞬间的最新 TF）变换整帧点云；
  融合定时器 20Hz，动态 TF 摆动可达 50°/s，几十毫秒延迟就会让远距离点错位数米。
- 原始点云没有逐点时间戳字段（只有 x/y/z/intensity），无法做标准逐点去畸变。

**解决（fusion v2，离线验证）：** 融合时按每个点云的“到达时刻 - 50ms”回查 TF
（50ms 是离线扫参最优值，等价于扫描中点时刻）。离线重算 164735 bag：

| 指标（点<-0.2m 比例） | 原录制 | 到达时刻 TF |
|---|---|---|
| 全部帧 | 1.36% | 0.27% |
| 运动段 | 4.32% | 0.75% |
| 静止段 | 0.11% | 0.04% |

修改文件：`src/spherical_robot_description/scripts/point_cloud_fusion.py`
（`tf_lookup_offset=0.05s`，可调）。

### 3.6 170307 bag 发现的新问题（fusion v3 + compensator 修正）

170307 bag（动态补偿 + fusion v2 实测）暴露三个问题：

1. **z 补偿启动饱和**：录制开始前 z_disp 已积到 -0.28m（应 ≈0），
   整个点云被压低约 30cm → 静止段 29% 点穿透。
   原因：z 零偏未初始化，启动瞬间 a_vert 偏移被积分到限幅。
   **修复**：首次进入 z 补偿时用当前 a_vert 初始化零偏；高通收敛加快到 ~5s；
   限幅收窄到 ±0.15m。
2. **融合输出骤减（144 → 32 帧）且 58s 后停摆**：
   同步检查用两雷达各自的 header 时间戳（内部时钟不同步，|差|<50ms 只有 49.6%）；
   tf2 过去时刻查询依赖缓冲历史。**修复（fusion v3）**：
   同步改用同一时钟的到达时刻；直接订阅 /tf 维护 2s 环形缓冲，
   按到达时刻-50ms 取最近变换；发布后清空缓存避免重复帧。
   离线重算：帧数 32 → 629。
3. **加速度计校正 10° 上限挡住大偏差**：本次录制静止时两雷达相对重力
   偏约 14°（164735 只有 1.1°，疑为测试地面不平或机器人被垫斜），
   14° > 10° 上限导致校正完全不生效，TF 保持标称姿态。
   **修复**：上限放宽到 30°，离线仿真 14° 偏差 5s 内收敛到 <0.7°。

> ⚠️ 注意：如果地面本身是斜面，平坦 grid 下点云必然“穿透”，
> 动态补偿只能保证雷达姿态与重力对齐，无法把斜面变平。验证时请用水平地面。

### 3.7 171525 bag：fusion v3 存错对象导致 0 帧（已修复并验证）

171525 bag 录制正常（原始点云 1244/1250 帧、/tf 24884 条、z 补偿已回到标称值），
但 `/merged_points` 为 **0 帧**。

原因：fusion v3 环形缓冲里存的是 `t.transform`（Transform 消息），
而 `_to_world()` 里访问 `tf.child_frame_id`（只有 TransformStamped 才有），
每次融合都抛 AttributeError，被 try 捕获后节点空转。

**修复**：环形缓冲改存完整 TransformStamped。

用修复后的逻辑离线重算 171525 bag：

| 指标 | 全部 (841帧) | 运动段 (263帧) | 静止段 (578帧) |
|---|---|---|---|
| %点<-0.2m 平均 | 0.04% | 0.09% | 0.02% |
| z_min 平均 | -0.665 | -0.707 | -0.645 |
| 最差帧 %<-0.2m | 1.60% | 1.60% | 0.10% |
| 静止地面拟合 | 倾斜 0.25°, offset 2.4cm | — | — |

对比：161906（静态 TF）运动段 8.3% → 164735（v3）4.2% →
171525（全部修复后离线）0.09%，基本消除。

### 3.8 Python 融合节点性能瓶颈 → C++ 重写（173115 bag）

173115 bag（fusion v3 + 全部修复实测）：

- 静止段：0.06% 点<-0.2m，地面倾斜 0.32°/0.21° —— 已经干净；
- 运动段：2.35% 点<-0.2m（从 8.3% 降下来，但未达离线 0.09%）；
- 真机只有 173 帧/105s，而离线重算可以有 728 帧。

原因：rclpy 给 `PointCloud2.data` 赋值约 **163ns/字节**，160k 点（2.5MB）
要 ~400ms，Python 融合节点物理上达不到 10Hz；executor 饱和导致
使用旧 TF、丢失配对。

**解决**：融合节点重写为 C++（`src/point_cloud_fusion_node.cpp`，
可执行文件 `point_cloud_fusion`），单帧处理 <5ms；Python 版改名
`point_cloud_fusion_py.py` 留作参考。launch 已指向 C++ 版。

### 3.9 174604 bag：C++ 节点两个 bug 修复

174604 bag（C++ 版首次实测）：

- 静止段 0.03% 点<-0.2m；运动段 1.45%（继续下降）；但输出只有 1.4Hz。

发现 C++ 节点两个 bug：

1. **merged.width 写错**：设成 `t1.width + t2.width`（192），
   但数据包含两雷达全部行（172800 点），RViz/read_points 只会读 192 点；
   **修复**：`width = t1.height*t1.width + t2.height*t2.width`。
2. **发布后清空缓存丢配对**：新 raw1 与下一帧 raw2 配对常超 50ms，
   导致频率只有 1.4Hz；**修复**：不清缓存，记录已融合时刻，
   任一侧有新点云就重新配对；同步窗口放宽到 80ms；定时器 20ms。

预期修复后输出 ~7-9Hz。

### 3.10 175142 bag：C++ 节点启动即崩溃（时间源不匹配）

175142 bag 中 `/merged_points` 完全没输出：C++ 节点启动后 ~0.6s
静默 SIGABRT（exit -6），launch 日志为空。

用节点自检（`FUSION_SELFTEST=1`）复现出异常：

```text
fuse exception: can't subtract times with different time sources [1 != 2]
```

**根因**：`last_fused1_/last_fused2_` 用默认构造的 `rclcpp::Time`
（时间源=RCL_ROS_TIME=2），与 `this->now()`（RCL_SYSTEM_TIME=1）相减
直接抛异常；异常从定时器回调冒泡 → std::terminate → SIGABRT。

**修复**：用 `this->now()` 初始化已融合时刻；同时给所有回调加 try/catch、
变换循环加越界保护、publisher QoS 改为 reliable（RViz 默认可靠订阅，
之前 best-effort 导致 RViz 收不到 /merged_points）。
自检已通过。

### 3.11 2026-08-05：球心离地高度修正 0.395 → 0.345

用户实测确认：base_link（球心）离地是 **345mm**（= 球半径），
之前代码里用的 0.395m 是错误值。

地面标定得到的“雷达总高”不变（rslidar_1 = 0.4143m，rslidar_2 = 0.4482m），
只需要把 0.05m 从 base_link 高度移到雷达 z 偏移：

| 项 | 旧值 | 新值 |
|---|---|---|
| world→base_link z | 0.395 | **0.345** |
| rslidar_1 z | 0.0193 | **0.0693** |
| rslidar_2 z | 0.0532 | **0.1032** |
| 合成总高 rslidar_1/2 | 0.4143 / 0.4482 | 不变 |

修改位置：

- `launch/dual_lidar_fusion.launch.py`：static_transform_publisher 0.345
- `src/point_cloud_fusion_node.cpp`：`WORLD_TO_BASE_Z` 0.345（Python 参考版同步）
- `urdf/spherical_robot.urdf`：两雷达 z +0.05
- `scripts/suspension_compensator.py`：URDF 表同步
- 文档：CALIBRATION.md / 记录.md / PROGRESS.md

点云输出（合成变换）完全不变；修正后 base_link 坐标系才符合真实球心位置。

### 3.12 2026-08-05：180158 / 100408 bag 均 0 帧（诊断中）

时间源修复后，C++ 节点能稳定运行（不再崩溃），但 180158 和 100408 两个 bag
里 `/merged_points` 仍为 0 帧。节点日志只有启动信息和一条 “TF history empty”，
之后无任何警告/异常，说明 fuse() 在静默早退（大概率没收到点云或时间差超窗）。

已做：

- 点云订阅改为 `QoS(10).reliable()`（与驱动发布端可靠一致）；
- 新增每 5s 状态日志：`clouds=收到的点云数/帧 tf=TF消息数 pub=发布数 |a1-a2|`，
  下次运行直接看状态行即可定位；
- 保持所有回调 try/catch。

待用户跑一次实时测试并回传状态日志。

**结论（已解决）**：状态日志显示节点其实在正常发布。0 帧根因是点云订阅 QoS：
驱动以 reliable 发布，融合节点用 best-effort（SensorDataQoS）订阅收不到数据。
改为 `QoS(10).reliable()` 后恢复（101536 bag 实测 3.72Hz、344 帧）。

### 3.13 2026-08-05：最终验证通过（101536 bag）

| 阶段 | 帧数 | %点<-0.2m | z_p1 |
|---|---|---|---|
| 静止段 | 221 | 0.04% | -0.010 |
| 运动段 | 123 | 0.65% | -0.145 |
| 全部 | 344 | 0.26% | — |

- 输出频率 3.72Hz（sync_window=0.08 限制，可放宽到 0.12 提频）；
- 地面拟合：静止 0.21°/0.26°，运动中 0.39°，offset ≤2cm —— 补偿全程有效；
- 最差帧出现在旋转速率 ~40°/s 的瞬间（单帧单 TF + 0.1s 扫描周期的物理极限）。

运动段穿透曲线：8.3% → 4.2% → 2.35% → 1.45% → **0.65%**。

### 3.14 2026-08-05：最新实测（102121 bag）

清理旧 bag 后唯一保留的 bag，也是当前最优结果：

| 阶段 | 帧数 | %点<-0.2m | z_p1 | 最差帧 |
|---|---:|---:|---:|---:|
| 静止段 | 285 | 0.06% | -0.042 | 1.00% |
| 运动段 | 315 | 0.24% | -0.095 | 6.98% |
| 全部 | 600 | 0.16% | -0.070 | 6.98% |

- 输出 600 帧 / 135.5s = **4.43Hz**；
- 运动段穿透继续降到 **0.24%**，为所有 bag 最优。

## 4. 当前代码/配置状态

### 4.1 静态外参（URDF，`src/spherical_robot_description/urdf/spherical_robot.urdf`）

```xml
<origin xyz="0.0 0.007 0.0693" rpy="-1.5946 0.0033 -3.1147"/>  <!-- rslidar_1 -->
<origin xyz="0.057 0.0069 0.0482" rpy="-1.5412 -0.0096 0.0301"/>  <!-- rslidar_2 (08-13) -->
```

`world → base_link`：`(0, 0, 0.345)`，旋转为单位四元数。

### 4.2 启动方式

- 静态外参（默认）：`bash start_lidar.sh`
- 动态补偿：`bash start_lidar.sh suspension:=true`
- launch 中 `suspension` 默认关闭；日常使用可 `suspension:=true` 开启动态补偿。

### 4.3 录制

```bash
bash record_bag.sh
```

自动重试，成功后会打印最终 bag 路径。录制流程：静止 30 s → 移动 30 s → 静止 30 s。

## 5. 关键数据（20260804_161906 bag）

- 静止段地面拟合：`z = -0.0058x + 0.0037y + 0.0033`，倾斜 0.39°。
- 运动段：地面模式 +0.229 m，深穿透集中在固定方位（-93°~-142°），越远越深。
- 运动前后 IMU 重力方向变化：rslidar_1 = 7.25°，rslidar_2 = 7.24°。
- 旧基线（20260730_165017）：135/135 帧 z_min < -0.2 m，平均 -3.0 m。
- 新标定静止段：仅 0.04% 点 < -0.2 m（z_min -0.5~-0.95 m，15~20 m 远稀疏点）。

## 6. 待办

- [x] 重写 suspension_compensator v3（持久姿态 + 加速度计漂移校正 + z 回弹补偿），离线仿真验证通过；
- [x] 融合节点 v2：按点云到达时刻回查 TF（离线验证运动段穿透 4.3% → 0.75%）；
- [x] 170307 问题修复：z 补偿启动初始化、fusion v3（到达时刻同步 + TF 环形缓冲）、加速度校正上限放宽到 30°；
- [x] 171525 修复：fusion v3 环形缓冲改存 TransformStamped（离线验证 841 帧、运动段 0.09%）；
- [x] 173115 实测：Python rclpy data 赋值瓶颈（~400ms/帧）→ 融合节点重写为 C++；
- [x] 174604 实测：C++ 节点 width 错误 + 缓存清空丢配对 → 修复（预期 7-9Hz）；
- [x] 175142 实测：C++ 节点 SIGABRT（rclcpp::Time 时间源不匹配）→ 修复并自检通过；
- [x] QoS 修复：点云订阅改 reliable（best-effort 收不到驱动数据）；
- [x] 最终验证（101536）：静止 0.04%、运动 0.65%、3.72Hz、地面全程水平；
- [ ] 可选：放宽 sync_window 提频到 5-6Hz，或做逐行去畸变 / FAST-LIO2；
- [ ] 根据实测调整 z 回弹补偿参数（泄漏时间常数、增益）；
- [ ] 评估 yaw 漂移是否需要磁力计/点云特征校正；
- [ ] 若 sqlite 崩溃反复出现，切换到 mcap 存储；
- [ ] 清理 `bags/_broken/` 中的残包。

## 7. 参考文件

- 分析脚本：`scripts/analyze_bag_z.py`、`scripts/analyze_motion_vs_penetration.py`
- 融合节点：`src/spherical_robot_description/scripts/point_cloud_fusion.py`
- 动态补偿：`src/spherical_robot_description/scripts/suspension_compensator.py`
- 录制脚本：`record_bag.sh`
- 启动脚本：`start_lidar.sh`

---

## 2026-08-11 回退至旧融合链路最终版（102121）

### 背景

此前在旧链路上叠加了 FAST-LIO2 / 新双雷达融合 / BEV / WebGL 浏览器查看等新代码。
经评估，当前需要回到“运动地面穿透 0.24%”的旧融合链路最终版（bag 102121 验证版本）。

### 操作

- git `main` 回退到 `c5d545b`（旧融合链路完整版），并提交回退说明 `ad61181`；
- 剔除：spark-fast-lio、rslidar_lio_adapter（新融合/轻量降采样/WebGL）、
  fastlio launch/配置、`start_fastlio.sh`、`start_web_view.sh`、`start_live_view.sh` 等；
- 新链路完整代码保留在 git 分支 `backup_new_lio_webgl_20260811`；
- 旧的 build/install/log 移到 `/tmp/build_new_lio_20260811` 等目录（未删除）；
- 旧链路 3 个包（rslidar_msg / rslidar_sdk / spherical_robot_description）重新编译通过。

### 当前状态

- 启动：`bash start_lidar.sh suspension:=true`
- 录制：`bash record_bag.sh`
- 回放：`bash play_bag.sh`（自动选最新 `dual_lidar_*` bag）
- 新增分析脚本：`scripts/analyze_merged_penetration.py`（102121 口径：点 z<-0.2m 占比，
  动态 TF >10°/s 或 IMU |ω|>0.08 rad/s 分段）
- 新 bag `dual_lidar_20260811_171219_r2`（74s / 184 帧 merged / 2.48Hz）已录制，
  **穿透分析待补**；原始两雷达自帧 0% 穿透。

### 恢复新链路（如需）

```bash
git switch backup_new_lio_webgl_20260811
colcon build
```

---

## 2026-08-13 双雷达共面 / 竖直面标定（低速）

### 背景

在 102121 旧融合链路基础上继续标定。目标从“两片点云完全融合”简化为：
**运动/静止的每一时刻，至少两雷达看到的地面要落在同一个平面上**，然后再压左右 Y。

- 最初两帧点云（`1.jpg` / `2.jpg`，见
  `snapshots/2026-08-13_低速共面标定/`）显示 Z 方向和 X/Y 方向同时存在刚性平移错位；
- 先做地面分割 + 最小二乘共面优化：能把 roll/pitch/z 收敛，最底端基本对齐，
  但左右 Y / yaw 仍然对不上；
- 改用障碍物 bag 做竖直面多平面标定和离线 ICP，受双雷达侧装、球体遮挡、
  重叠区窄的影响，纯 ICP 容易局部最优，最终采用**手动调参 + 离线结果人工确认**。

### 本次新增离线脚本（不修改运行时文件）

| 脚本 | 作用 | 典型 bag |
|---|---|---|
| `scripts/calibrate_ground_coplanar.py` | 低速帧地面共面，优化 rpz/full | `160447` |
| `scripts/calibrate_planes_offline.py` | 地面 + 竖直面多平面联合标定 | `140142_r2` / `150428_r2` |
| `scripts/calibrate_icp_offline.py` | 重叠区 6DoF ICP（需人工确认） | `133316_r2` |

### 08-13 录制 bag 用途

| Bag | 用途 / 状态 |
|---|---|
| `dual_lidar_20260813_133316_r2` | 低速共面 + ICP 数据 |
| `dual_lidar_20260813_140142_r2` | 正前方障碍物静止 bag，两雷达对同一障碍物有重叠，用于多平面 |
| `dual_lidar_20260813_142858` | 竖直面多平面标定尝试 |
| `dual_lidar_20260813_150428_r2` | 前方大障碍物验证 |
| `dual_lidar_20260813_160447` | 最新分割地面 + 共面优化 |
| `dual_lidar_20260813_162921_r3` | 当前外参重新录制，**待分析** |

### 最终参数（08-13，已写入并编译）

```xml
<origin xyz="0.0 0.007 0.0693" rpy="-1.5946 0.0033 -3.1147"/>   <!-- rslidar_1 -->
<origin xyz="0.057 0.0069 0.0482" rpy="-1.5412 -0.0096 0.0301"/> <!-- rslidar_2 -->
```

同步位置：

- `src/spherical_robot_description/urdf/spherical_robot.urdf`
- `src/spherical_robot_description/scripts/suspension_compensator.py`（`URDF` 表）
- 手动调参文件备份：`snapshots/2026-08-13_低速共面标定/tuned_calib.yaml`

修改后已 `colcon build --symlink-install`；`start_lidar.sh` 启动时会打印当前
`rslidar_2` 外参，方便确认版本。

### 已知残余

- 低速/静止最底端已对齐，但**左右 Y 方向仍有残余偏差**，继续用竖直面多平面压；
- 运动段动态穿透仍需用新 bag 验证；
- 机械臂 / MoveIt 与 lidar_ws 同时运行会发布两个 `base_link`，导致 RViz 显示异常，
  跑球形机器人前先停机械臂或换 `ROS_DOMAIN_ID`。

### Foxglove Studio

Foxglove Studio 2.9.0（arm64）已通过官方 deb 系统级安装（`/usr/bin/foxglove-studio`），
并配置官方 apt 源用于后续升级。启动：`foxglove-studio`。
如要订阅 ROS2 话题，需再装 `ros-humble-foxglove-bridge`。

---

## 2026-08-13 晚间：运动地面对齐验证 + 补偿器缺帧容错

### 背景

16:51 录制运动 bag（开了 RViz）发现运动时两雷达最底端地面不对齐。
先用新脚本 `scripts/analyze_ground_misalign.py` 逐帧量化（不经过融合节点）：

- 16:51：静止 Δz0 = −0.7±3.9cm，运动 Δz0 = −12.1±22.1cm，最差 −86cm；
- 该包录制 IMU 严重缺帧（IMU1 631 条 / IMU2 481 条，大量 >0.2s 缺口，最大 2.7s），
  最初怀疑是补偿节点缺 IMU 导致姿态滞后。

### 重要更正：IMU 缺口是录制端丢包，不是补偿问题

逐样本核对后确认：

- 16:51 包（旧代码，无外推）在“录制缺口”期间，TF 姿态依然平滑转动
  （~20-30°/s 无停顿），说明补偿节点实际拿到了连续 IMU；
- 17:19 包（新代码）补偿日志无任何“IMU 缺口”警告，同样说明节点端无缺口；
- 开着 RViz 时录制丢包更严重（16:51 IMU 10Hz vs 17:19 14-17Hz vs 17:34 IMU2 187Hz），
  属 rosbag2 录制端在负载下的丢包行为。

因此补偿器的 dt 截断（旧代码 `dt>0.1s 强制 5ms`）不是运动错位的主因。

### 代码改动：补偿器 IMU 缺帧容错（保留为鲁棒性改进）

`suspension_compensator.py` 增加：

- 旋转积分 dt 上限 2.0s：IMU 真缺帧时把漏掉的转动补回来（不再截断到 5ms）；
- z 回弹积分单独限步长 0.3s，防止缺帧后 z 跳变；
- TF 发布时若 IMU 超过 50ms 无新样本，用最近角速度外推姿态（最多外推 1s，
  角速度 <0.02rad/s 不外推）；
- 缺口 >0.3s 打 `IMU 缺口` 警告日志（限频 5s）。

### 排除的假设（均有数据）

- TF 查询偏移 0~0.5s 扫描：两雷达 z0 几乎不变 → 不是时间对齐问题；
- 按扫描列/行线性去畸变（0.1/0.2s 扫描窗）：结果完全一致 → 不是简单扫描畸变；
- 地面坡度回归：对 lidar2 仅解释 23% 方差 → 不是主要因素；
- z 补偿差：与 Δz0 相关 0.79，运动时均值只差 −3cm → 解释约 3cm；
- 两雷达 TF 相对旋转误差：约 3°，静止/运动恒定 → 静态标定残余（Y 方向待办），
  不是补偿发散。

### 剩余现象

- lidar2 剧烈运动帧原始点云经变换后有 30% 点在 −10cm 以下、中位数在 3m 高，
  融合节点同时刻输出同样有 15.5% 低点 → 来自传感器/原始数据侧，非 ROS 变换；
- 深低点集中在 >1rad/s 滚动帧，接近物理极限。

### 验证结果（173447，无 RViz，直线运动；结论已于 08-14 更正，见文末）

| 指标 | 值 |
|---|---:|
| 静止 Δz0 | −1.3±4.7cm |
| 运动 Δz0 | −3.0±6.5cm |
| 最差帧 | −17.1cm（@1.17rad/s） |
| 运动与速率相关性 | −0.11 |

结论：运动地面对齐验证通过（目标：运动均值 <5cm、std ≤6.5cm）。
> ⚠️ 08-14 更正：该结论只适用于**直线运动**；真实转弯为球体翻滚，见文末更正章节。

### 待办

- 压左右 Y / yaw 残余（两雷达 TF 相对旋转约 3°）：`calibrate_planes_offline.py --dof lateral`；
- z 回弹静止饱和：静止时 z_disp 冲到 +10~15cm 限幅（两雷达都有），
  建议静止时加快泄漏/收紧限幅；
- IMU1 驱动侧输出 17:34 仅 21Hz（IMU2 187Hz），需确认驱动状态；
- 162921_r3 的穿透率/频率分析仍待补。

## 2026-08-14 更正：173447 只验证直线运动；真实转弯（球体翻滚）仍系统性错位

### 修正背景

08-13 晚把 173447 判为“运动地面对齐验证通过”。08-14 确认运动模式差异：

- 173447 是**直线运动**（球体不翻滚）；
- 171903 是**球体翻滚转弯**（真实转弯模式）；
- 球体转弯时中央驱动轮滚动带动球体翻滚，两侧 lidar 模块绕底座偏转（惯性 + 弹性复位），
  会出现一边 lidar 贴近地面、另一边远离；
- 安装结构确认：两雷达侧装、底座贴球面、本体沿球面径向朝外，类比“人站在地球上”
  （脚踩球面、头朝外，局部“下”≈指向球心）。

### 数据对比（analyze_ground_misalign.py）

| bag | 模式 | 静止 Δz0 | 运动 Δz0 | 最差帧 |
|---|---|---|---|---|
| 171903 | 翻滚转弯（真实） | −2.2±8.7cm | **−15.8±8.5cm** | −35.6cm（lidar1 +12.8 / lidar2 −22.8，一高一低） |
| 173447 | 直线运动 | −1.3±4.7cm | −3.0±6.5cm | −17.1cm |

- 171903 全段 Δz0 与旋转速率相关系数 −0.61，最差帧集中在翻滚段；
- 173447 运动段相关系数 −0.11，错位小且与速率无关 → 直线模式正常。

### 171903 不能直接当干净基线

- 52s 内 IMU1 14Hz / IMU2 17Hz（设计 200Hz），>0.2s 缺口 50+ 个，最大 2.4s；
- 录于 17:31 缺帧容错补丁之前，补偿器在缺口期间没有补算/外推；
- 173447 中 IMU2 202Hz 正常，但 IMU1 仍只有 21Hz → IMU1 驱动/链路输出偏低是真问题。

### 结论与下一步

- 验收口径改为“真实翻滚转弯 bag”：用最新补偿器（含缺帧容错）重录转弯数据，
  先排查/修复 IMU1；
- 补偿器改造方向：按“底座贴球、本体沿球面径向朝外”的运动学，由模块自带 IMU 姿态 +
  球半径/安装几何直接算 x/y/z，替代 z 泄漏双积分；
- 原“验证通过”结论仅对直线运动成立，README / PROGRESS / BAG分析汇总 已同步修正。

## 2026-08-14：离线仿真与补偿器 v4（地面平面慢速反馈）

### 思路调整

按“底座贴球、本体沿径向朝外”的运动学建模前，先用 171903/173447 数据量化
错位来源：

- lidar1 其实基本正常（171903 运动段 z0 +2.4±4.3cm、平面倾角 ~0.5°）；
- lidar2 运动段 z0 −13.6±5.6cm —— 主要错位是**垂直平移**；
- z 残差与姿态角线性模型 R²≈0.19（lidar2），说明纯几何模型解释力弱；
- 直接用“自身地面平面残差”做慢速反馈（τ=0.5s）更有效。

### 离线仿真结果（scripts/analyze_ground_feedback.py）

| Bag | 模式 | 运动 Δz0 修正前 | 修正后 | 修正后 Δroll/Δpitch |
|---|---|---|---|---|
| 171903 | 翻滚转弯 | −15.8±8.5cm | **−0.1±4.0cm** | 0±0.75°/0±0.92° |
| 173447 | 直线 | −3.0±6.5cm | **0.0±3.0cm** | ~0±1° |

反馈量：lidar1 fb_z −2.2±3.5cm、lidar2 fb_z +7.5±8.8cm（最值 ±21cm）；
roll/pitch 修正 ≤~2°/9°。说明补偿量落在可控范围，闭环可行。

### 实现（suspension_compensator v4）

- 订阅 `/rslidar_points_1/2`，用当前 TF 变换到 world 拟合地面平面；
- `fb_z/fb_roll/fb_pitch` 慢速 EMA（τ=0.5s）回灌 TF：z ±0.20m、角度 ±10° 限幅；
- 地面拟合门限：内点 ≥300、法线竖直分量 ≥0.90；不满足则保持上次修正；
- launch 新增 `ground_feedback:=true/false`（默认开）；
- z 泄漏双积分保留作地面不可见时的惯性兜底。

### 集成验证（离线回放 171903）

- 节点正常订阅点云/IMU、拟合地面、更新反馈并发布 TF，接线与门限逻辑通过；
- 注意：bag 内 IMU 缺帧（14/17Hz、最大 2.4s）在回放时被“补算”放大，
  出现姿态漂移上百度和反馈限幅 —— 这是回放假象，不代表真机（录制端丢包
  不影响当时补偿节点）。

### 待办

- 修 IMU1 驱动/链路输出（14~21Hz → 200Hz）；
- 用 v4 补偿器 + 连续 IMU 录制真实翻滚转弯 bag，验收运动段 Δz0 均值 <5cm；
- 实机验证通过后再决定 z 泄漏双积分去留，以及是否需要把反馈移到 C++ 节点降延迟。

## 2026-08-14：113628 仿真录制验证与补偿器 v6c（达到 <5cm 目标）

### 新 bag 数据

`dual_lidar_20260814_113628`（08-14 上午仿真环境录制；34s，IMU1 200.0Hz 无缺口 /
IMU2 180.5Hz 2 个缺口；
点云 ~10Hz，merged 9.45Hz）：

| 分段 | Δz0 | Δroll/Δpitch |
|---|---|---|
| 静止 | −1.1±1.9cm | ~0.3° |
| 运动（平均 0.91 rad/s） | +1.0±8.2cm | ~0±1.5° |

### 根因定位

- 两雷达拟合平面 z0 呈 ~0.3Hz 反相摆动（原始 corr −0.48，带通后 −0.81）；
- 用“地面带内点中位数高度”独立验证：两雷达各 ±3.4cm、corr −0.40 → **真实
  悬挂反相回弹**，不是拟合假象；
- 原始点云 1% 分位两雷达几乎不相关（+0.08），排除“单纯深点异常”；
- 拉长双积分时间常数（v6：τ_v=10s/τ_h=20s）静止即饱和 ±20cm 失败 → 双积分
  无法简单跟 0.3Hz 回弹；
- **v6c 有效：地面反馈 τ=0.08s + 反馈有效时冻结 z 双积分**。

### v6c 结果（离线回放 113628 + 补偿器 + 分析录制 TF）

| 指标 | v4 | v5 | v6c |
|---|---|---|---|
| 运动 Δz0 | +1.0±8.2cm | −0.3±8.3cm | **−0.5±4.4cm** |
| 静止 Δz0 | −1.1±1.9cm | −1.1±2.1cm | **−1.2±1.2cm** |
| 最差帧 | ±18cm | ±18cm | **−14.1/+11.8cm** |

分档（v6c）：<0.2 rad/s ±1.2cm；0.2–1.5 rad/s ±3~4cm；1.5–3.0 rad/s ±5.3cm；
>3.0 rad/s ±7.0cm（n=18）→ 剩余偏差集中在高速段，属扫描畸变。

### 改动归因（哪个改动有效、结果如何）

| 版本 | 改动内容（相对上一版） | 运动 Δz0 | 归因结论 |
|---|---|---|---|
| v4 | 新增地面反馈 τ=0.5s（z 双积分仍活动） | +1.0±8.2cm | 消除系统性 −15cm 偏差，但 0.3Hz 反相回弹残留 |
| v5 | 反馈有效时冻结 z 双积分（τ 仍 0.5s） | −0.3±8.3cm | **无改善** → z 双积分不是 0.3Hz 回弹的直接原因 |
| v6 | z 双积分 τ_v/τ_h 拉长到 10s/20s（恢复活动） | 失败（静止饱和 ±20cm） | **负结果** → 拉长双积分无法简单跟 0.3Hz，已回退 |
| v6b | τ 0.5→0.15s（保持冻结双积分） | −0.6±5.4cm | **第一次显著改善**（8.3→5.4cm） |
| **v6c** | **τ 0.15→0.08s（保持冻结双积分）** | **−0.5±4.4cm** | **达标**（5.4→4.4cm），最差帧 ±14cm |

**结论：使效果变好的关键改动是“加快地面反馈 EMA 时间常数 τ：0.5s → 0.15s → 0.08s”**，
运动段 Δz0 标准差依次 8.3 → 5.4 → 4.4cm。单独冻结 z 双积分（v5）没有改善；
单独拉长双积分（v6）直接失败。因此冻结 z 双积分只是**配套防干扰措施**
（防止双积分与快速反馈叠加），真正生效的是反馈带宽。

同一组数据的严格对照：v4/v5/v6b/v6c 都用同一个 113628 bag 离线回放补偿器，
再对录制 TF 做相同口径地面拟合，改动前后只有补偿器参数差异，结果可比。

### 备份与清理

- 快照：`snapshots/2026-08-14_地面反馈v6c/`；
- 删除 /tmp 回放测试 bag（v5/v6/v6b/v6c 共 ~3.6GB）；
- 删除已淘汰 bag：`_broken`、`20260805_102121`、`20260811_171219_r2`、
  `20260813_162921_r3`、`20260813_165151_r2`（约 12.4GB，bag 不入 git 不可恢复，
  其结论均已记录在本文档）。

## 2026-08-14 下午：133832_r2 录制最终验证（v6c，无代码改动）

同一个 v6c 参数在新录制 bag 上验证，效果比离线回放更好：

| 指标 | 113628 回放（v6c） | **133832_r2 录制（v6c）** |
|---|---|---|
| 静止 Δz0 | −1.2±1.2cm | **−1.2±0.5cm** |
| 运动 Δz0 | −0.5±4.4cm | **−0.9±2.4cm** |
| merged 穿透 | — | 静止 0.21% / 运动 0.24% / 全部 0.23% |
| merged 频率 | 9.45Hz | **19.0Hz** |

- 133832_r2：IMU1 187.9Hz（6 缺口）/ IMU2 17.8Hz（78 缺口，最大 1.44s）；
  IMU2 低频率仍属录制端丢包（TF 99Hz 平滑），不影响补偿与本节分析；
- 分档：<0.2 rad/s ±0.5cm；0.2–0.5 ±1.5cm；0.5–1.0 ±2.8cm；1.0–1.5 ±2.4cm；
  1.5–3.0 ±2.5cm —— 全转速段 <3cm，达到并超过 <5cm 目标；
- 唯一大偏差帧：t=34.78s（0.74rad/s）Δz0=−15.9cm，该帧 lidar2 地面拟合被
  深点污染（l2_z1=−109cm），属单帧拟合异常而非系统性错位；
- 无代码改动，进一步确认 **v6c 参数（FB_TAU=0.08 + 反馈有效时冻结 z 双积分）
  是当前最优**；下一步仍是 >1.5 rad/s 的逐点去畸变（可选优化，录制已达标）。

## 2026-08-14 下午：融合节点 v2（逐点去畸变 + 地面共面校正）

### 改动

`point_cloud_fusion_node.cpp` 新增两个参数化功能（默认开）：

- `deskew`：逐点去畸变。Airy 点云是规则网格（height=96），同一列共享方位角，
  用**列号**估算扫描时刻（不再用单帧单 TF），对 21 个时间 bin 做 TF 插值
  （slerp + 平移），消除 0.1s 扫描内高速旋转造成的点云畸变；
- `plane_align`：融合前双雷达地面共面校正。两片点云分别两遍最小二乘拟合地面
  （先粗拟合，剔除 >5cm 深点异常，再精拟合），把 lidar2 的 roll/pitch/z
  做 EMA 对齐到 lidar1（增益 0.30），保证 fused 底端共面。

### A/B/C 对照（113628 回放 + v6c，同一补偿器）

| 配置 | 穿透均值 | 穿透 max | 残差std 均值 | p95 | max |
|---|---:|---:|---:|---:|---:|
| A：旧行为 | 0.54% | 7.56% | 2.12cm | 2.82cm | 3.46cm |
| C：仅去畸变 | 0.56% | 5.23% | 2.16cm | 2.88cm | 3.26cm |
| B：去畸变+共面 | **0.53%** | **3.45%** | **2.07cm** | **2.49cm** | **2.91cm** |

### 归因与结论

- **共面校正贡献最大**：B vs C，穿透 max 5.23→3.45%，残差 p95 2.88→2.49cm；
- 去畸变单独仅改善尾部（A vs C：max 7.56→5.23%），均值中性；
- 第一版共面拟合用普通最小二乘，被深点异常带偏（稳态 z 修正 4.29cm 且无改善），
  改成“两遍拟合 + 剔除 >5cm 异常”后稳态修正降到 1.28cm，指标才转好
  （2.22→2.07cm）——**拟合鲁棒性是共面校正生效的前提**；
- 均值残差 ~2.1cm 已接近单帧拟合噪声 + 扫描畸变的地板，融合层再优化收益有限；
- 新增 `scripts/analyze_merged_planarity.py` 用于 merged 穿透/共面度分析。

## 2026-08-14 下午：140619 录制验证（融合 v2 + 补偿器 v6c 全链路）

`dual_lidar_20260814_140619`（75s，IMU1 194Hz / IMU2 19Hz 录制端丢包；
点云 ~10Hz，merged 1175 帧 ~16.5Hz）：

| 指标 | 结果 |
|---|---|
| merged 穿透率 | 全部 **0.22%**（max 4.63%），z_p1 −0.090 |
| merged 地面残差 | 均值 **1.83cm**、p95 2.30cm、max **2.75cm** |
| TF 级静止 Δz0 | −0.6±0.3cm |
| TF 级运动 Δz0 | −0.1±2.9cm |

- 为目前最优：比 113628 回放（2.07cm）和 133832（TF 运动 2.4cm）更进一步，
  融合 v2 的共面校正 + 去畸变在真实录制数据上生效；
- 运动段唯一大偏差帧 t=34.92s（Δ+19.3cm）是 lidar1 地面拟合被深点污染
  （l1_z1 −86cm）的单帧异常，融合层残差 max 只有 2.75cm；
- 结论：**当前推荐配置 = 补偿器 v6c（地面反馈 τ=0.08 + 冻结 z 双积分）
  + 融合 v2（deskew + plane_align 默认开）**。

## 2026-08-14 晚：融合 v2.1（去畸变时间轴修复 + 深点畸变保护）

### 发现去畸变 bug

检查点云组织：Airy 输出 **height=900 × width=96**，一行 96 个激光通道共享同一
方位角，时间沿**行**推进（第 0 行 yaw≈0°，第 450 行 yaw≈180°，第 899 行≈360°）。
v2 去畸变误用列号（p%width），时间映射等于错乱，所以去畸变均值无收益。

修复：按行号 `frac = (p/width)/(height-1)`。113628 回放（干净 IMU，同口径）：

| 配置 | 穿透均值 | 穿透 max | 地面残差均值 | max |
|---|---:|---:|---:|---:|
| v2（列号去畸变 + 共面） | 0.53% | 3.45% | 2.07cm | 2.91cm |
| **v2.1（行号去畸变 + 共面）** | **0.41%** | 4.88% | **1.98cm** | 3.07cm |

均值改善（0.53→0.41%、2.07→1.98cm），最差帧基本持平 → 还需处理高速极端帧。

### 深点畸变保护（outlier_filter）

140619 最差帧 t≈50.5s（4.8 rad/s）有 zmin −5m 的穿地假点，穿透 4.63%。新增：
融合时对 t1/t2 用鲁棒拟合的地面平面，剔除低于平面 **0.25m** 的点（默认开，
`outlier_below` 可调）。离线验证（140619 merged 同口径）：

| 阈值 | 穿透均值 | 穿透 max |
|---|---:|---:|
| 不过滤 | 0.22% | 4.63% |
| 0.50m | 0.20% | 4.08% |
| 0.35m | 0.18% | 3.66% |
| **0.25m（默认）** | **0.14%** | **3.08%** |
| 0.20m | 0.11% | 2.54% |

选 0.25m 作为默认（每帧仅滤 ~0.14% 点，对平面地面安全；如遇台阶/坑洼需调大）。

### 其他

- 用户放入工作区的 `存档/` 含重复 src 包导致 colcon 编译失败，已加
  `存档/COLCON_IGNORE` 解决（未改动存档内容）；
- 140619 回放验证不可用（bag 内 IMU2 仅 19Hz，回放时补偿器漂移 43°），
  A/B 结论以 113628 回放 + 140619 离线过滤为准。

## 2026-08-14 下午：152736 全链路最终验证（v6c + 融合 v2.1）

IMU 干净的新录制（IMU1 196.7Hz / IMU2 189.2Hz），融合 v2.1 全链路：

| 指标 | 结果 |
|---|---|
| merged 帧数 / 频率 | 1302 / 16.1Hz |
| 穿透率 | 静止 0.07%、运动 0.17%、全部 **0.12%**（max 2.51%） |
| 地面残差 | 1.76cm（p95 2.03 / max 2.30cm） |
| TF 级 Δz0 | 静止 −0.7±0.3cm、运动 −0.2±3.5cm |

- 相比 140619（0.22%/max 4.63%/1.83cm）：穿透再降一半，最差帧降到 2.51%，
  地面残差 1.76cm —— **v2.1 的两个修复（行号去畸变 + 深点保护）在实机生效**；
- 最差帧已不在高速段（运动 max 1.75% @0.91rad/s），而是 t≈36s（静止 2.51%）
  和 t≈61s（低速 1.5~1.7%）——疑似场地地面特征（台阶/凹坑/杂物），需现场确认；
- 当前推荐配置不变：补偿器 v6c + 融合 v2.1（默认参数）。

## 2026-08-14 晚：FAST-LIO2 单 LIO 回放验证（备份分支）

- 分支 `backup_new_lio_webgl_20260811`（独立工作区 `/home/wz/lidar_ws_fastlio`）
  恢复并验证方案A；
- 修复 1：IMU 乱序——adapter 与 spark-fast-lio 的 IMU 话题改 reliable
  （best_effort 回放乱序 → `IMU loopback` 清缓冲、odom 出不来）；
- 修复 2：坐标系——实测 odom→base_link 初始为单位阵，world_anchor 保持
  base 重力小角度旋转，odom 就绪后按均值 z 重发校准版；
- 验证（新 XYZIRT bag `fastlio_20260814_161821`，59s）：
  `/cloud_registered_base` + TF 链 → 地面残差 **1.57cm**（p95 2.11/max 2.80）、
  穿透 **0.17%**（max 5.35% 高速帧）、平面高 **−4.6cm** 固定偏移；
- 备注：`/cloud_registered`（odom 系）不能直接做地面分析，双 LIO 融合须用
  `/cloud_registered_base`；gravity_alignment 保持关闭；
- 已录制 `fastlio_20260814_172859` 待回放验证；下一步双 LIO。
